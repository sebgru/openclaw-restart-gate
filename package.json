{
  "name": "docker-restart-gate",
  "version": "0.1.0",
  "private": false,
  "description": "Allowlist-only, authenticated Docker restart gateway with cooldowns.",
  "type": "module",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
